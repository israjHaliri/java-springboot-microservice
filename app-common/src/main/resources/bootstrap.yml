spring:
  application:
    name: cleverly-book
  cloud:
    config:
      uri: http://localhost:8001

server:
  port: 8004

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8002/eureka/